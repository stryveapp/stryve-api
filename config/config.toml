env = "dev"             # dev / test / prod
debug = true            # true / false
log_level = "debug"     # debug / info / warn / error / fatal / panic
log_format = "text"     # text / json
log_path = ""
log_filename = "stryve-api.log"
port = 3000

[databases]
[databases.dev]
host = "192.168.50.50"
port = 5432
ssl_mode = "disable"
name = "stryve"
username = "stryve"
password = "secret"

[databases.test]
host = "localhost"
port = 5432
ssl_mode = "disable"
name = "stryve_test"
username = "stryve"
password = "secret"

[databases.prod]
host = "192.168.50.50"
port = 5432
ssl_mode = "disable"
name = "stryve"
username = "stryve_prod"
password = "secret"
